0) В форме в файле регистрации добавить загрузку картинки (аватар пользователя)
   Кроме username и pass в базу сохранится имя файла 
   При логине в welcome добавить к слову Привет вывод <img src="и здесь адрес его фотки"/> 

1) Внутри файла welcome.php Добавить форму с полем type="text" и кнопку submit "создать папку".
При подтверждении формы создается папка внутри текущей папки user-а (в коде она называется $userfolder)
$userfolder состоит из getcwd() и id user-a. Id User-а находится в массиве $_SESSION

2) На странице welcome есть select со списком user-ов. Сейчас при нажатии отправить ничего не происходит.
   Нужно добавить обработку события submit этой формы 
(либо события onchange (или onselect) у самого <select> )
При подтверждении отправляется ajax запрос в контроллер и обновляется дерево файлов и папок ниже.

3) Рядом с файлами поставить значек удалить - можно воспользоваться span, у которого будет id=".. "
   При клике на span в контроллер отправляется запрос с {action: delete и filename: ....}   
   На стороне php используется функция unlink, аргументом которой будет filename  
   Сначала можно попробовать на файлах в текущей директории, так как будет достаточно только имени файла
   без указания полного правильного пути к нему

4) Загрузка нескольких файлов - для этого в форме upload поставить name

5) Проверка расширения файла через 
	Либо - функция explode разбивает файл на массив из двух элементов - до точки и после,
	вторым его элементов станет расширение файла.
	Либо - mime_content_type('php.gif') вернет image/gif
	       mime_content_type('test.php'); вернет text/plain
   При нажатии на файлы text/plain - в textarea выдается content ( это работает сейчас )
   При нажатии на файлы image/gif - показать картинку 

6) Сделать кнопку logout - Для того, чтобы выйти, нужно закончить сессию? 
   Либо удалить из сессии id user-а и перезагрузить страницу	

7) Как понять, сколько пользователей сейчас залогинено на сайте? (сессия создает файлы)
